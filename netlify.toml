# Netlify configuration for BetaPower static site

[build]
  command = "npm run build"
  publish = "out"

# Security headers are defined in public/_headers file
# and will be automatically applied by Netlify

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
